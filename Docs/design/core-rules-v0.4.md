# 핵심 룰 v0.4 — 쪼꼬미 공성전 (완성판 1차)

작성일 2026-09-26 (9절까지 같은 날 갱신). v0.3 (실시간 공격 디펜스) 위에 **타워 합성**, **팀전 밸런스**, **후반 압박**, 그리고 그림·움직임·이펙트·소리·타이틀/결과 화면까지 얹어 "한 판 온전히 돌아가는 게임"으로 만든 판이다. v0.3 문서의 규칙은 여기서 바꾼 것 말고는 그대로다.

## 1. 게임 이름·화면

- 이름: **쪼꼬미 공성전** (Chokkomi Siege). 아기자기한 픽셀 chibi 스타일, 크림색 UI.
- 씬은 `Assets/Scenes/Main.unity` 하나. 타이틀(모드 선택 → 1v1 / 2v2 / 3v3, 게임 방법, 합성표, 소리 설정) → 경기 → 결과(전적·통계, 다시 하기 / 타이틀로).
- 2v2·3v3 는 **봇 팀원**과 함께 한다 (온라인은 아직). 사람은 팀 0 의 0번 자리. 팀원 타워에는 P2·P3 표시.

## 2. 타워 합성 (새 규칙)

### 2-1. 별 합치기 (같은 타워 3개)

| 별 | 조건 | 공격 배율 | 사거리 |
|---|---|---|---|
| ★1 | 기본 | ×1 | 기본 |
| ★2 | 같은 정의·같은 별 ★1 × 3 | **×2.2** | +0.5칸 |
| ★3 | ★2 × 3 (= ★1 9개) | **×5** | +1칸 |

- 재료는 **같은 주인**의 타워여야 한다 (팀원 것과는 못 합침). 자리는 클릭한 타워 자리, 즉시 완성(건설 시간 없음). 재료 중 하나라도 강화면 강화 유지.
- 오라(드루이드·드론)도 별을 따라 ×1.5 / ×2.
- 판매 환불 = 비용 × 3^(별−1) × (강화 2) × 80%.
- 의도: 3개 합이 DPS ×3 인데 ★2 는 ×2.2 → DPS 는 손해지만 **슬롯 2칸을 되찾고** 사거리가 늘고 강화 비용이 하나로 줄어든다. 롤토체스처럼 "같은 걸 모으는" 재미.

### 2-2. 레시피 합성 (다른 타워 2개 → 새 타워, 8종)

| 결과 | 재료 | 비용(합) | 특징 |
|---|---|---|---|
| 불화살 사수 (불·궁수) | 나무 궁수 + 화염 창탑 | 13 | 공9 1.2/초 사3.5 대공 화상 3/초 |
| 가시 폭발꽃 (숲·마법사) | 가시 덤불 + 불꽃 술사 | 15 | 공10 0.6/초 사2 광역1.5 감속30% |
| 화염 방사기 (기계·마법사) | 태엽 포탑 + 불꽃 술사 | 17 | 공7 1.6/초 사2.5 광역0.8 화상 3/초 |
| 거목 투석기 (숲·전사) | 강철 대포 + 숲의 드루이드 | 25 | 공30 0.3/초 사4 광역1.2 거인 2배 |
| 번개 비행선 (기계·궁수) | 불사조 + 수리 드론 | 22 | 공14 0.8/초 사4 대공 공중 2배 광역0.6 |
| 저격 드론 (기계·궁수) | 나무 궁수 + 수리 드론 | 14 | 공22 0.5/초 사5.5 대공 |
| 철갑 가시 (기계·전사) | 가시 덤불 + 강철 대포 | 21 | 공18 0.5/초 사2 감속40% 거인 2배 |
| 화염 정령 (불·마법사) | 숲의 드루이드 + 불꽃 술사 | 19 | 공6 0.8/초 사2.5 광역1 주변 타워 공격 +20% |

- 순서 무관, 같은 주인, 결과는 클릭한 타워 자리에 ★1 로 즉시. 합성 타워끼리는 다시 합성 못 하지만 같은 합성 타워 3개로 ★은 올릴 수 있다.
- 합성 타워는 상점에 없다. 계열·직업이 재료와 달라지므로 시너지 짜기가 퍼즐이 된다.
- 봇: 빈 슬롯이 2칸 이하가 되면 별 합치기 → 레시피 합성 순으로 시도한다.

### 2-3. 조작

타워 클릭 → 아래 행동 패널: [강화 N골드] [판매 +N] [★n 합치기] [합성 → 결과 이름]. 우클릭은 바로 판매. [합성표] 버튼(상단·타이틀)에서 레시피를 본다.

짝이 여럿이면 버튼에 "(고르기)"가 붙고, 누르면 후보 타워가 하늘색으로 깜빡인다. 합성은 짝 하나를, ★ 합치기는 소모할 두 개를 차례로 클릭한다 (자리가 중요하니까). ESC 나 다른 곳 클릭으로 취소. 명령은 `Fuse(a, b)` / `MergeWith(a, m1, m2)` 로 고른 타워 id 가 그대로 실린다 (온라인도 동일).

## 3. 밸런스 (봇 대전 스윕, SimpleBot vs SimpleBot{Aggression=80}, 16판)

### 3-1. 인원별 기본값 (바뀜)

| | 1v1 | 2v2 | 3v3 |
|---|---|---|---|
| 라인 폭 | 4 | **5** (전 6) | **6** (전 8) |
| 기지 체력 | **40** (전 60) | **120** (전 90) | **300** (전 120) |
| 기본 웨이브 규모 | ×1 | **×1.3** (전 1.6) | **×1.3** (전 2.2) |

- 폭이 넓을수록 타워 사거리 커버가 옅어져 팀전이 일방적으로 기지 파괴로 끝났다 (3v3 폭 8: 16/16 파괴, 평균 5분). 폭을 줄이고 기지 체력을 인원 비례보다 크게 올렸다.
- 결과: 1v1 파괴 0/16 (평균 누수 20/40, 접전), 2v2 파괴 1/16, 3v3 파괴 9/16 (평균 578초). 팀전은 보내기가 3배로 쌓이므로 아직 파괴가 잦다. 사람 플레이 뒤 조정.
- 처치 골드를 "팀원 전원 지급"으로 바꿔 봤더니 봇이 그 돈으로 더 보내서 오히려 파괴가 늘었다 → 원래대로 **팀 균등 분배** 유지.

### 3-2. 후반 압박 (새 규칙)

10번째 기본 웨이브부터 웨이브마다 **기본 웨이브 체력 +8%** (19번째 웨이브 +80%). 봇 대전에서는 결과에 거의 영향이 없었지만(봇 방어가 합성으로 같이 강해짐), 사람이 방어를 소홀히 하면 후반에 새기 시작하도록 둔다. `LaneConfig.LateWaveIndex/LateWaveStepPercent`.

### 3-3. 합성 사용량

1v1 20판에서 별 합치기 567회(★3 20회), 레시피 합성 545회 — 봇이 슬롯이 차면 자주 쓴다. 사람 기준으로는 "언제 합칠지"가 새로운 판단거리.

## 4. 연출·소리 (코드로 만든 자산)

- **그림**: `Tools/art/gen_sprites.py` (Pillow) 가 `Assets/Resources/Sprites/` 에 151장 생성. 타워 17종 × (대기 2프레임 + 공격), 유닛 11종 × 걷기 2프레임, 타일·성·성문, 투사체 10, 이펙트 22, UI 35, 로고·타이틀 배경. 64 PPU, 포인트 필터 (`ArtImporter`).
- **소리**: `Tools/audio/gen_audio.py` (numpy) 가 `Assets/Resources/Audio/` 에 효과음 33 + 음악 3 (타이틀·전투·긴장) 생성. 이벤트 중엔 긴장 곡으로 바뀐다.
- **움직임**: 유닛 걷기 프레임·그림자·공중 부유, 타워 숨쉬기·깜빡임·공격 반동, 투사체 종류별 속도·포물선·회전, 타격 스파크·화상 불꽃·사망 폭발·연기, 합치기/합성 별 폭발, 기지 흔들림, 떠오르는 데미지 숫자·골드 팝업.
- 다시 만들려면: `python3 Tools/art/gen_sprites.py && python3 Tools/audio/gen_audio.py` (Pillow, numpy 필요).

## 5. 조작 요약

타워 상점 클릭 또는 Q~O · 빈 칸 클릭 = 짓기 · 타워 클릭 = 강화/판매/합치기/합성 · 우클릭 = 판매 · D = 뽑기 · 카드 클릭 = 보내기 · 스페이스 = 정지 · 1/2/3 = 배속 · ESC = 메뉴.

## 6. 남은 것

- 온라인 2v2·3v3 (락스텝: 명령만 교환, 코어는 이미 결정론).
- 해금(메타), 팀 핑·송금 UI (송금 명령은 코어에 있음).
- 사람 플레이테스트 뒤 밸런스: 팀전 기지 체력, 합성 타워 수치, 증강 강도.
- 그림 품질 손보기 (스크립트 수정 후 재생성하거나 그린 PNG 로 교체 — 파일 이름만 맞추면 된다).

## 7. 온라인 (v0.5, 2026-09-26 추가)

### 7-1. 방식: 호스트 중계 락스텝

- 코어 시뮬은 결정론(정수 연산, 시드)이므로 **명령만 교환**한다. 상태는 보내지 않는다.
- 호스트가 **4틱(0.2초)마다 턴**을 만든다 = 그 사이에 들어온 모든 명령(자기 것 + 클라들 것 + 봇 것). 턴을 모두에게 보내고 자기도 적용한다.
- 클라는 받은 턴이 있어야 그 틱을 진행한다. 턴이 안 왔으면 기다리고("대기…" 표시), 3턴 이상 밀리면 빠르게 따라잡는다. 호스트는 아무도 기다리지 않는다 → 느린 클라가 호스트를 멈추지 못한다.
- 명령 지연 = 네트워크 왕복 + 최대 0.2초. 실시간 타워 디펜스라 체감 문제 없음.
- 25턴(5초)마다 호스트가 상태 해시를 실어 보내고, 클라는 자기 해시와 비교한다. 다르면 "[어긋남!]" 표시 (지금은 표시만, 복구는 나중).
- 클라가 보낸 명령의 팀·자리는 호스트가 그 클라의 슬롯으로 **덮어쓴다** (속이기 불가).
- 봇은 호스트만 돌리고 그 명령도 턴에 실린다. 사람이 나가면 그 자리는 호스트 봇이 이어받는다. 호스트가 나가면 판이 끝난다.
- 온라인에서는 정지·배속이 없다 (증강 10초 정지는 시뮬 안의 규칙이라 그대로).

### 7-2. 전송

- 지금은 **TCP 직결** (포트 27015). 같은 LAN 이면 호스트의 192.168.x.x 로 바로, 인터넷이면 호스트 공유기 포트포워딩 필요. 스팀 P2P 로 바꿀 땐 `INetTransport` 만 구현하면 된다.
- 메시지: Hello(이름) → Welcome(플레이어 id) → Lobby(인원수·플레이어·슬롯) … Start(시드·인원수·슬롯·턴 길이) → Commands(클라→호스트) / Turn(호스트→모두) / Ping·Pong / Leave / Chat.

### 7-3. 로비

호스트가 인원수(1v1/2v2/3v3)를 정하고, 사람 이름을 눌러 다음 빈 자리로 옮긴다(팀 바꾸기). 들어오는 사람은 첫 빈 자리부터 채운다: 1v1 이면 두 번째 사람이 상대, 2v2 이면 팀원. 빈 자리는 봇. 채팅 있음.

### 7-4. 검증

- 코어 테스트: 루프백으로 호스트·클라 세션이 한 판을 끝까지 돌려 **해시가 같음** (1v1, 2v2), 클라 명령 재태깅, 나간 사람 봇 대체, TCP 루프백 양방향 프레임.
- PlayMode: 로비 화면 생성, 호스트 경기 화면이 스스로 턴을 만들어 진행하고 클라 시뮬이 턴만으로 같은 상태가 됨.
- 실제 두 대 사이 플레이테스트는 아직. 같은 기계에서 빌드 두 개를 띄워(127.0.0.1) 확인 가능.

## 8. 첫 사람 플레이테스트 반영 (v0.5)

지현 피드백: "맵이 너무 짧다, 오는 걸 보고 대응할 시간이 있었으면", "초반부터 조금씩 샌다".

원인 (단일 라인 실험, 궁수만 지었을 때 웨이브별 누수):
- 유닛이 타워 사거리 안에 머무는 시간이 짧았다 (격자 5~8칸, 사거리 3 → 늑대가 사거리 안에 7초).
- 3웨이브 박쥐는 대공 타워(궁수·불사조)가 없으면 무조건 샌다. 4웨이브 보스 오우거(체력 540)는 궁수 6개로도 2 누수.

바꾼 것:

| | v0.4 | v0.5 |
|---|---|---|
| 라인 길이 | 20칸 | **30칸** |
| 타워 격자 위치 | 5~7칸 | **10~12칸** (라인 중간, 앞 10칸은 "오는 걸 보는" 구간) |
| 유닛 속도 배율 | 60% | **50%** (늑대 30초, 거북 86초에 통과) |
| 타워 공격 배율 | 150% | **140%** |
| 보내기 비용 배율 | 125% | **100%** (방어가 세진 만큼 보내기를 싸게 → 공격·방어 비율 유지) |
| 3v3 기지 체력 | 300 | 360 |

결과 (봇 스윕 16판): 1v1 누수 평균 16/40 (2:30까지 5), 판당 보내기 120회 (전 78회) — 초반은 잘 막히고, 보내기는 더 자주 한다. 2v2 파괴 3/16, 3v3 파괴 11/16 → 360 으로 올림.
궁수 6개(36골드)면 8웨이브까지 기본 웨이브를 다 막고, 궁수 4개 + 대포면 보스도 막는다. 대공 없는 조합(창탑·덤불만)은 여전히 박쥐에 샌다 — 의도.

## 9. 구불구불한 맵 (v0.6)

지현 피드백: "배치 칸을 늘리고 구석구석 두게", "일자 말고 원작처럼 구불구불한 구간이 있어야 효율적인 배치를 고민하는 재미가 있다".

### 9-1. 맵 = 격자 + 경로

- 맵은 격자(1v1 14×9, 2v2 18×12, 3v3 22×12). **경로**는 뱀 모양으로 가로 3~4줄을 3칸 간격으로 오가며, 유닛은 경로를 따라 걷는다 (진행도 = 밀리칸).
- **경로가 아닌 모든 칸이 타워 자리**(1v1 84칸). 원작처럼 아무 데나 짓되, 경로에서 멀면 쓸모없고 **굽이 안쪽 구석**은 세 구간을 동시에 본다 → 자리 고르기가 곧 전략. 봇도 "사거리 2.5 안 경로 칸 수"가 큰 자리부터 짓는다.
- 맵은 ASCII 로 정의한다 (`MapDef.FromAscii`): `.` 자리, `#` 경로, `S` 출발, `B` 기지, ` ` 빈 땅. `MapDef.Snake(w, h, runs)` 가 뱀 맵을 만든다. 손으로 그린 맵을 넣으려면 문자열만 추가하면 된다.
- 직업 시너지는 "같은 줄"이 사라져 **8방향 이웃에 같은 직업 타워**가 있으면 켜지는 것으로 바꿨다.
- 화면: 내 진영 왼쪽, 상대 진영 오른쪽 (나란히). 유닛은 방향에 따라 좌우를 본다.

1v1 맵 (위가 북):
```
..............
S############.
............#.
............#.
.############.
.#............
.#............
.############B
..............
```

### 9-2. 밸런스 (봇 스윕 16판)

굽이 구석 자리가 강해서 같은 공격 배율(140%)이면 누수가 7/40 로 뚝 떨어졌다 → **타워 공격 140% → 115%** (1v1 누수 평균 ~17/40, 2:30까지 1). 팀전은 누수가 기지 체력에 비해 너무 적어 **기지 2v2 120 → 80, 3v3 360 → 160**. 경로가 41칸이라 늑대 41초, 거북 2분 — 대응 시간은 충분하다.

## 10. 타워 역할 (v0.7)

지현 질문: "타워마다 광역·원거리 같은 역할이 있으면 좋겠다, 그건 조합으로 하는 건가?" → 기본 타워부터 역할이 갈리게 수치를 벌리고, 조합은 역할을 **합치는** 것으로 정리.

| 기본 타워 | 역할 | 수치 |
|---|---|---|
| 나무 궁수 | 단일·대공 | 공4 1.0/초 사3 |
| 가시 덤불 | 근접 광역·감속 | 공3 1.2/초 사1.5 광역1 감속30% (v0.7: 광역 추가) |
| 숲의 드루이드 | 지원·공격 오라 | 주변 +15% |
| 화염 창탑 | 화상 | 공4 1.0/초 사2 화상 3/초×3초 |
| 불꽃 술사 | 광역 | 공7 0.6/초 사2 광역1.2 |
| 불사조 | 대공 특화 | 공8 0.9/초 사3.5 공중 2배 |
| 태엽 포탑 | 원거리 저격 | 공14 0.4/초 사4.5 |
| 강철 대포 | 소광역 강타·대거인 | 공24 0.3/초 사3 광역0.7 거인 2배 |
| 수리 드론 | 지원·공속 오라 | 주변 기계 +25% |

- 새 공격 방식 **연쇄**(`ChainCount/ChainRange10/ChainPercent`): 맞은 유닛에서 1.5칸 안 다른 유닛으로 튄다. 번개 비행선(불사조+수리 드론)이 3회 70%.
- 역할은 수치에서 자동 분류(`TowerRoles.Of`)해서 상점 아이콘·선택 설명·행동 패널·합성표에 "[광역·감속]"처럼 보인다. 테스트로 기본 9종의 역할이 서로 다른지 확인한다.
- 광역 타워는 투사체가 닿을 때 폭발 이펙트.
- 밸런스: 광역이 늘어 팀전 누수가 줄어 기지 2v2 60 / 3v3 100 으로 낮춤 (1v1 40 유지, 봇 스윕 누수 19/16).

## 11. 해금 (v0.8)

v0.3 13절의 "여러 판에 걸쳐, 조건 비공개" 해금을 넣었다. 프로필(`Profile`)은 전적·해금·달성한 미션을 `persistentDataPath/profile.txt` 에 남긴다. 판이 끝나면 `MatchSummary` 로 조건을 검사하고, 새로 열린 것을 결과 창에 "★ 해금!" 으로 보여준다. 타이틀의 [해금 도감]에는 잠긴 것은 `???` 와 힌트만.

| id | 조건 (비밀) | 보상 |
|---|---|---|
| first_win | 첫 승리 | 칭호 새내기 지휘관 |
| flawless | 누수 0 승리 | 맵 굽이 다섯 (14×15, 긴 뱀 길) |
| star3 | ★3 타워 | 비밀 증강 별의 축복 (★2+ 타워 공격 +10%) |
| fuse4 | 한 판에 합성 4종 | 비밀 증강 연금술 (합치기·합성 결과 즉시 강화) |
| horde60 | 한 판에 60마리 보내기 | 고수 봇(공격성 85) + 칭호 군단장 |
| cliff | 기지 5 이하로 승리 | 칭호 벼랑 끝 |
| team_win | 2v2·3v3 승리 | 맵 굽이 둘 (14×6, 짧고 빠름) |
| online_win | 온라인 승리 | 칭호 온라인 정복자 |
| veteran10 | 10판 | 비밀 증강 노련함 (뽑기 -1, 손패 +1) |
| all_missions | 비밀 미션 8종 전부 | 칭호 비밀 사냥꾼 |

- 비밀 증강은 해금한 것만 증강 뽑기 풀에 들어간다 (`MatchConfig.AllowedSecretAugments`). 해금 맵·고수 봇은 타이틀에서 고른다 (혼자 하기 1v1 만).
- 온라인 (v0.8.1): 호스트가 로비에서 자기가 해금한 맵을 고르면 Lobby·Start 메시지에 실려 모두 같은 맵을 쓴다. 비밀 증강은 **각자 자기가 해금한 것만** 뽑기 풀에 들어간다 — 클라가 Hello 에 해금 목록을 실어 보내고, 호스트가 슬롯별로 모아 Start 에 싣는다 (`MatchConfig.SecretAugmentsPerSlot`). 모두 같은 Start 를 받으므로 결정론이 유지된다.

## 12. 템포 (v0.9)

지현 피드백: "진행 속도가 생각보다 느리다. 유닛도 많이 보내고 진행 속도를 높이자."

| | 전 | 후 |
|---|---|---|
| 경기 시간 | 10분 | **8분** |
| 시작 골드 / 기본 인컴 | 40 / 8 | **50 / 10** |
| 수입 간격 | 20초 | **15초** |
| 뽑기 비용 / 손패 | 5 / 6장 | **4 / 8장** (+ ×3 뽑기 버튼, F) |
| 보내기 비용 배율 | 100% | **85%** |
| 유닛 속도 배율 | 50% | **65%** (뱀 길 41칸이라 늑대 32초, 대응 시간은 남는다) |
| 기본 웨이브 | 30초 뒤부터 30초마다 | **20초 뒤부터 24초마다** (19웨이브가 7:32 에 끝남) |
| 건설 시간 | 2초 | **1초** |
| 증강 / 이벤트 | 3:00·6:00 / 4:30·7:30 (60초) | **2:30·5:00 / 3:30·6:00 (45초)** |
| 타워 공격 배율 | 115% | **150%** (유닛이 빨라져 사거리 안 시간이 줄고 보내기가 싸진 만큼) |
| 기지 체력 | 40 / 60 / 100 | 40 / **100 / 180** |

봇 스윕(16판): 1v1 판당 보내기 **120 → 332회**, 누수 23/40, 2:30까지 2~4, 기지 파괴 4/16 (평균 458초). 2v2·3v3 는 파괴 10/16 안팎 — 팀전은 결판이 잘 난다. 사람 플레이 뒤 다시 조정.

## 13. 후반 공격 스케일 (v0.10)

지현: "게임이 진행될수록 방어는 두터워지는데, 공격 유닛도 점점 비싸지고 단단해지는 게 나와야 하지 않나?" — 맞다. 세 가지.

### 13-1. 시간에 따른 뽑기 등급 확률 (v0.11 에서 14절의 레벨제로 대체)

| 시각 | 일반 | 희귀 | 영웅 | 전설 |
|---|---|---|---|---|
| ~2:00 | 70 | 25 | 5 | 0 |
| 2:00~4:00 | 55 | 30 | 15 | 0 |
| 4:00~6:00 | 42 | 32 | 20 | 6 |
| 6:00~ | 32 | 32 | 24 | 12 |

`WaveCatalog.DrawOdds(seconds)`. 화면의 뽑기 안내에 현재 확률이 보인다.

### 13-2. 전설 등급 4종 (4:00 부터)

| 유닛 | 계열 | 비용 | 체력 | 속도 | 누수 | 인컴 | 능력 |
|---|---|---|---|---|---|---|---|
| 맹수 왕 | 야수 | 20 | 340 | 1.8 | 4 | 4 | 은신 2초 |
| 리치 | 암흑 | 22 | 300 | 1.0 | 4 | 5 | 치유 5/초 (2.5칸), 4초마다 침묵 2초 |
| 고대 드래곤 | 공중 | 26 | 560 | 1.2 | 5 | 6 | 비행, 등장 시 3칸 침묵 2초 |
| 타이탄 | 거인 | 24 | 760 | 0.7 | 6 | 5 | 감속 면역 |

마지막 기본 웨이브(19)에 타이탄이 온다. 카드 테두리는 보라(`ui_card_legend`).

### 13-3. 돌격 강화 (팀 공유, 원작의 업그레이드)

[돌격 강화] 버튼: 25, 40, 55 … 골드 (최대 6레벨). 레벨마다 우리 팀이 **보내는** 유닛 체력 +6%, 속도 +2%. 기본 웨이브엔 적용 안 됨. 봇은 분당 1레벨쯤 산다.

### 13-4. 밸런스

후반 공격이 세지므로 타워 공격 배율과 기지 체력을 같이 올렸다: **타워 공격 200%, 기지 1v1 50 / 2v2 170 / 3v3 300** (v0.11 레벨제 뒤 팀전 기지 상향).

봇 스윕(16판, 1v1): 파괴 11/16, 평균 453초, 누수 38~42/50, 2:30까지 1, 판당 보내기 300회. 결판이 잘 나면서 초반은 막힌다.
실험 메모: 등급 확률 이동이 결판의 주 원인(전설 없이도 비슷), 돌격 강화는 처음 +12%/8레벨이면 봇이 매분 사서 6:30 안에 전부 파괴 → +6%/6레벨·90초에 1회로 줄임. 사람 플레이 뒤 재조정.


## 14. 레벨과 경험치 (v0.11)

지현: "확률이 후반에 자동으로 오르는 게 아니라, 롤토체스처럼 턴마다 주는 경험치 + 돈으로 레벨업해서 전설 확률을 올리자."

- **개인 레벨** 1~9 가 뽑기 등급 확률을 정한다. 경험치는 **수입 때마다 +2** (15초, 자동), **4골드로 +4** (버튼·E 키). 넘치는 경험치는 이월.
- 필요 경험치: 2, 4, 8, 12, 20, 30, 44, 60 (레벨 1→2 부터). 자동만으로는 8분에 약 레벨 6, 골드를 부으면 6분 안에 9.

| 레벨 | 일반 | 희귀 | 영웅 | 전설 |
|---|---|---|---|---|
| 1 | 80 | 20 | 0 | 0 |
| 2 | 70 | 28 | 2 | 0 |
| 3 | 60 | 32 | 8 | 0 |
| 4 | 50 | 34 | 14 | 2 |
| 5 | 42 | 34 | 19 | 5 |
| 6 | 35 | 33 | 23 | 9 |
| 7 | 28 | 32 | 27 | 13 |
| 8 | 22 | 30 | 30 | 18 |
| 9 | 15 | 28 | 32 | 25 |

- 골드 쓸 곳이 넷: 타워 / 뽑기·보내기 / 돌격 강화(팀) / 경험치(개인). "지금 방어냐, 경제냐, 다음 등급이냐"가 매 수입마다의 선택.
- 봇은 여유 골드가 있으면 30초마다 경험치를 사고, 레벨이 시간에 뒤처지면 더 자주 산다.
- 시간 기반 전설 해금(`LegendUnlockSeconds`)은 없앴다. 레벨 4부터 전설.


## 15. 레벨 증강 (v0.12)

지현: "레벨 1·3·6·9 에 증강을 고르게, 총 4번."

- 증강은 시간표(2:30·5:00, 10초 정지)가 아니라 **레벨 1·3·6·9 에 닿는 순간** 그 사람에게 3장이 온다. 레벨 1 은 시작 직후.
- 사람마다 시점이 다르므로 **게임을 멈추지 않는다**. 20초 안에 안 고르면 첫 장이 자동 선택. 고르는 동안 다음 증강 레벨에 닿으면 이전 것을 자동 선택하고 새로 준다.
- 화면은 지도 아래쪽 가운데의 얇은 띠(820×196)에 3장 가로로. 지도가 가려지지 않는다.
- 레벨 9 까지 가야 4번째 증강이므로, 경험치에 골드를 붓는 이유가 하나 더 생긴다.
- `MatchConfig.AugmentLevels`, `AugmentChoiceSeconds`. 예전 시간표 필드는 없앴다.
- 시작 직후 증강(유산 +25 등)으로 판이 더 빨라져 기지 체력 1v1 60 / 2v2 200 / 3v3 350 으로 올림. 봇 스윕 1v1: 파괴 15/16, 평균 6:12 — 봇은 서로 최대 공격이라 사람 플레이 뒤 재조정.

## 16. 3단계 타워 (v0.13)

지현: "합성한 것들을 재료로 또 합성하는 게 있으면 좋겠다."

2단계(합성) 타워 둘을 다시 합치면 3단계. 재료 합이 27~47골드라 중후반의 큰 목표가 된다. 3단계는 더 합성하지 못하지만 같은 것 3개로 ★은 올릴 수 있다.

| 3단계 | 재료 (2단계) | 비용 | 특징 |
|---|---|---|---|
| 유성 저격수 (불·궁수) | 불화살 사수 + 저격 드론 | 27 | 공40 0.6/초 사6 대공·공중 2배 화상 5/초 |
| 가시 거목 (숲·전사) | 가시 폭발꽃 + 철갑 가시 | 36 | 공34 0.5/초 사2.5 광역2 감속50% 거인 2배 |
| 화산 심장 (불·마법사) | 화염 방사기 + 화염 정령 | 36 | 공14 1.6/초 사3 광역1.5 화상 4/초 오라 +25% |
| 천둥 요새 (기계·궁수) | 거목 투석기 + 번개 비행선 | 47 | 공45 0.4/초 사5 광역1.5 연쇄 4회 대공·공중 2배·거인 2배 |

- 3단계 4종의 재료 8종은 서로 겹치지 않는다 (2단계 8종이 정확히 두 개씩 짝). 합성표는 2단계 8줄 + 3단계 4줄(어두운 줄).
- `FindRecipe` 는 단계에 상관없이 (재료 A, B) 쌍으로 찾는다. 봇도 2단계 둘이 있으면 3단계로 합친다.

### 16-1. 밸런스 메모

- 봇을 고쳤다: 기본 9종을 돌아가며 짓고(레시피 짝이 생기도록), 짝이 생기면 바로 합성, 타워 8개부터 ★ 합치기. 그러자 봇 방어가 훨씬 세져 1v1 누수가 51 → 2 로 떨어졌다. **합성 타워 배율(85/75%)은 결과를 거의 안 바꿨고**, 오라(드루이드·드론)와 계열 시너지가 섞인 구성이 원인.
- 타워 공격 배율 200 → **160%**, 2단계 85%, 3단계 75% (`Tier2DamagePercent/Tier3DamagePercent`). 봇 스윕: 1v1 누수 6/11 (60 중), 파괴 0/16, 2:30까지 3, 판당 보내기 258, 3단계 3개/판. 2v2 파괴 9/16, 3v3 7/16.
- 결론: "잘 짓는 방어" 대 "최대 공격"은 아직 방어가 유리하다. 사람 플레이에서 보내기가 무력하게 느껴지면 다음 손잡이는 (1) 전역 공격 배율 140, (2) 돌격 강화 +8%, (3) 오라 수치.


## 17. 손패 조합 → 히든 유닛 (v0.14)

지현: "뽑고 나서 보내지 않고 들고 있을 때 특정 조합이 완성되면 합성되어, 뽑기에서는 뽑을 수 없는 상위 히든 유닛이 나오면."

- 조합이 완성되면 재료 카드가 빛나고, **R 이나 [손패 합성] 버튼으로 합친다** (v0.15: 자동 → 수동. 늑대를 따로 보낼지 모아서 합칠지 고를 수 있게). 명령 `HandFuse(resultId)`. 히든은 등급 `Hidden` 이라 뽑기 풀에 절대 안 나온다.
- 조합은 발견 전엔 합성표에 `???` 와 힌트만. 한 번 만들면 프로필에 남아 재료가 보인다. 6종 다 찾으면 칭호 "조합의 달인".

| 히든 | 재료 | 비용 | 체력 | 누수 | 인컴 | 능력 | 힌트 |
|---|---|---|---|---|---|---|---|
| 거대 늑대 | 늑대 ×3 | 10 | 220 | 3 | 3 | 빠름 | 같은 야수 셋 |
| 하피 여왕 | 박쥐 + 그리핀 | 11 | 260 | 3 | 3 | 비행, 은신 2초 | 날개 둘 |
| 산 거인 | 거북 + 오우거 | 12 | 620 | 5 | 4 | 감속 면역 | 느린 것 둘 |
| 그림자 군주 | 암살자 + 어둠 사제 | 15 | 320 | 4 | 4 | 은신 3초, 치유 | 어둠 둘 |
| 재앙의 용 | 드래곤 + 흑마법사 | 26 | 900 | 7 | 7 | 비행, 등장·주기 침묵 | 영웅 둘 |
| 고블린 공성차 | 고블린 ×2 + 거북 | 11 | 360 | 4 | 4 | | 초록 둘이 등딱지를 타면 |

- 보내기 비용은 재료 합보다 싸고(들고 있는 대가), 체력·누수는 재료 합보다 크다 → "보낼까, 들고 있을까"가 새 선택.
- `WaveCatalog.HandRecipes`, `FindHandRecipe`, `MatchSim.TryHandFusion`, 이벤트 `HandFused`.


## 18. 조작 (v0.15 → v0.16 롤토체스식)

지현: "타워는 선택이 아니라 커서를 올린 상태에서. 비슷한 것은 같은 키로(유닛·타워 합성 = 같은 키). 판매는 롤토체스처럼 E. 전체적으로 롤토체스 유저에게 친숙하게, 건설은 스타크래프트처럼."

| 키 | 동작 (전부 커서 아래 대상) |
|---|---|
| D | 뽑기 |
| F | 레벨업 경험치 (4골드 → +4) |
| W (카드 위) | 그 카드 보내기 |
| E (타워 위) | 판매 (우클릭도 판매) |
| R | 합성: 타워 위면 그 타워의 레시피 합성, 아니면 손패의 완성된 조합 (커서 아래 카드가 속한 조합 우선) |
| C (타워 위) | ★ 합치기 (같은 타워 3개) |
| U (타워 위) | 강화 |
| B | 건설 목록. 열린 동안 Q W E R A S D F G 가 기본 타워 9종 (스타의 B 서브메뉴) |
| 상점 클릭 / 목록 클릭 | 같은 동작 (마우스만으로도 됨) |
| 고스트 상태에서 빈 칸 클릭 | 짓기. 시프트+클릭 = 계속 짓기. 우클릭·ESC = 취소 |
| 타워 클릭 | 행동 패널(같은 버튼들, 키 표시) |
| 스페이스 / 1 2 3 / ESC | 정지 / 배속 / 취소·메뉴 |

- 짝이 여럿이면(같은 타워 4개 이상, 합성 짝 둘 이상) 고르기 모드: 후보가 하늘색으로 빛나고 클릭으로 고른다.
- 커서에 뜬 타워(고스트)는 지을 수 있으면 초록, 아니면 빨강. ×3 뽑기는 뺐다.


## 19. 별·합성·강화 규칙 정리 (v0.16)

지현: "강화는 더 진화가 불가능한 최종 ★3 타워에만. 2성·3성 타워는 같은 성급끼리만 합성, ★2 를 합치면 ★2 합성 타워."

- **합성은 같은 별끼리만**: ★1+★1 → ★1 합성 타워, ★2+★2 → ★2 합성 타워, ★3+★3 → ★3. 별이 다르면 짝으로 안 보인다.
- **강화(공속 +50%, 타워 비용)는 ★3 타워만**. 더 올릴 게 없을 때 골드로 마지막 한 발.
- 이에 맞춰: 정예 증강 = "★3 강화 +80%", 연금술 = "★3 이 되는 순간 공짜 강화", 순혈 미션 보상 = 골드 +20·기지 +2 (전엔 계열 전원 강화).
- 봇도 ★3 만 강화한다. 합성 결과의 강화 유지 규칙은 그대로 (재료 중 하나라도 강화면 유지 — ★3 끼리 합성할 때만 의미).


## 20. 팀전 타워 공유 모드 · 손패 확장 (v0.17)

지현 (첫 플레이테스트 뒤): "뽑았을 때 칸이 너무 부족하다. 방어 타워는 서로 공유하면서 하는데 조합도 공유할지 각자 할지 — 차라리 모드를 선택할 수 있게, 공유 모드와 공유 아닌 모드로. 공격 유닛(손패)은 공유하지 않는다."

### 타워 공유 모드 (`MatchConfig.SharedTowers`, 팀전에만 의미)

| | 각자 (기본) | 공유 |
|---|---|---|
| 합성·★합치기 재료 | 내 타워끼리만 | 팀 타워 전부 (팀원 것과도) |
| 강화·판매 | 내 타워만 (전엔 판매·강화에 주인 검사가 빠져 있었다 → 이번에 막음) | 팀 타워 누구나 |
| 결과 타워의 주인 | 나 | 기준 타워(커서를 올린 것)의 주인 |
| 결과에 붙는 개인 증강 (정예·대공망·별의 축복·연금술) | 내 것 | 결과 타워 주인의 것 |
| 판매 환불 | 나 | 지은 사람에게 (남의 타워를 팔아 돈을 가져가진 못한다) |
| 손패·손패 조합 | 각자 | 각자 (변화 없음) |

- 판단은 전부 `MatchSim.CanUse(tower, player)` / `SameOwnerOrShared(a, b)` 한 곳에서. 봇도 같은 함수로 공유 모드면 팀원 타워를 재료로 쓴다.
- 고르는 곳: 오프라인은 타이틀 오른쪽 선택지 "팀전 타워: 각자/공유" (`GameSession.SharedTowers`), 온라인은 호스트 로비의 버튼 (`LobbyInfo.SharedTowers` → `StartInfo.SharedTowers`, `NetSession.SetSharedTowers`). 1v1 에선 켜 둬도 무시.
- 화면: 내 진영 제목에 "타워 공유 모드"/"각자 타워", 커서·클릭·단축키가 팀원 타워에도 먹고, 행동 패널 이름 옆에 "(팀원 P2 타워)".
- 봇 스윕 (16판, 3v3 은 8판): 공유 모드가 누수를 줄인다 (봇들이 서로 재료를 합쳐서). 2v2 누수 150/153 → 115/124, 3v3 218/309 → 123/192. 승률은 같다. 밸런스 값은 안 건드렸다 (지현이 플레이해 보고 말하기로).

### 손패 10장 + 레벨 보너스

- `HandMax` 8 → **10**, `HandBonusLevels = {3, 6, 9}` 마다 +1 (최대 13, 노련함 +1 별도). 히든 조합을 노리려면 3장을 묶어 들고 있어야 해서 8장은 실제로 5장쯤이었다.


## 21. 처음 하는 사람 돕기: 튜토리얼·팁·표시 (v0.18)

지현: "이것저것 생각해야 할 게 많다. 튜토리얼이라든지, 사람들이 알기 쉽게 적응하기 쉽게."

### 튜토리얼 판 (`GameSession.Tutorial`, 타이틀 첫 버튼, `-tutorial`)
- 1v1, 증강·이벤트·미션 없음(`FunLayer=false`), 시작 골드 90, 첫 웨이브 75초·간격 30초, 5분. 상대 봇은 `SimpleBot{Aggression=25, QuietSeconds=75}` — 75초까지 뽑기·보내기·돌격 강화를 안 하고 타워만 짓는다.
- 7단계, 각 단계는 **실제로 그 명령이 성공하면**(이벤트) 넘어간다: 짓기(Built) → 뽑기(Drew) → 보내기(Sent) → 합성(Fused, 나무 궁수의 레시피 짝을 알려줌) → ★합치기(Merged) → 레벨업(Level≥3) → 자유(12초 뒤 끝). 게임은 정지하지 않는다.
- 위쪽 패널(맵이 그만큼 아래로 밀린다, `_topPx` 150)에 단계·설명·[건너뛰기]. 다음에 만질 곳(상점·뽑기·손패·레벨업)을 노란 테두리가 깜빡이며 가리킨다.
- 끝나면 `Profile.TutorialDone` (타이틀 버튼이 회색이 되고 1v1 이 초록으로). 튜토리얼 판은 전적·해금에 안 들어간다.
- `TutorialGuide` (Game/Tutorial.cs). 테스트: PlayMode 에서 사람 자리를 봇이 두면 100초 안에 3단계 넘어가는지.

### 상황 팁 (`CoachTips`, 프로필 `Tips`, 타이틀 선택지 "도움말 팁")
- 판마다 종류별 한 번, 6초 간격: 합성 짝 생김 / 같은 타워 3개 / 손패 가득 / 골드 60+ 를 15초 방치 / 첫 누수 / 레벨 4 전설 안내. 0.5초마다 훑는다.

### 타워 위 R·C 표시
- 내 타워(공유 모드면 팀 타워) 중 지금 합성이 되는 것엔 R, ★합치기가 되는 것엔 C 배지 (`LaneRenderer.ActionFlags`, MatchView 가 5틱마다 `FuseOptions/MergeCandidates` 로 채운다). 롤토체스의 "합칠 수 있음" 반짝임에 해당.
